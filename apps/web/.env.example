# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dream_analyzer"

# AI Configuration
# Provider: "anthropic" or "openrouter" (default: anthropic)
AI_PROVIDER="anthropic"

# Anthropic API (if using anthropic provider)
ANTHROPIC_API_KEY="sk-ant-..."

# OpenRouter API (if using openrouter provider)
OPENROUTER_API_KEY="sk-or-..."

# AI Model (optional, uses provider default if not set)
# Anthropic examples: claude-sonnet-4-20250514, claude-3-5-sonnet-20241022
# OpenRouter examples: anthropic/claude-3.5-sonnet, openai/gpt-4-turbo, google/gemini-pro-1.5
AI_MODEL=""

# Auth.js (NextAuth v5)
# AUTH_SECRET は必須: openssl rand -base64 32 で生成
AUTH_SECRET="your-secret-here"
# AUTH_URL はVercelでは自動設定されるが、ローカル開発時は設定
AUTH_URL="http://localhost:3000"

# Google OAuth
# Get these from: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-client-secret"
